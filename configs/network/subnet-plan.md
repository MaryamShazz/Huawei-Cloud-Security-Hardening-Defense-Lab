# Subnet Plan (Configs Copy)

Canonical version: [`modules/02-vpc-network-security/subnet-plan.md`](../modules/02-vpc-network-security/subnet-plan.md). Kept here as a pointer so `configs/` stays a complete reference set without duplicating content that needs to stay in sync in two places.
