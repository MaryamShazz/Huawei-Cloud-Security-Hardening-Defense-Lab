{
  "description": "Example Cloud Trace Service configuration template.",
  "tracker_name": "system-tracker",
  "scope": "account-wide",
  "log_targets": [
    "iam:*",
    "vpc:*",
    "security-group:*",
    "cloud-firewall:*",
    "obs:*",
    "ecs:*"
  ],
  "storage_destination": {
    "bucket": "PLACEHOLDER_LOG_BUCKET",
    "encryption": true,
    "bucket_policy": "write-only-for-cts-service, no-delete-for-standard-accounts"
  },
  "retention_days": 365,
  "notes": "Log destination bucket should not be writable or deletable by the accounts whose actions are being logged."
}
