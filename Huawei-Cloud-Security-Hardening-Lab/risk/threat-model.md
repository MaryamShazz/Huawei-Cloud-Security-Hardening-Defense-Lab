# Threat Model (Risk Folder Copy)

Canonical version: [`docs/threat-model.md`](../docs/threat-model.md). Kept here as a pointer so everything under `risk/` (threat model, risk assessment, residual risk register) is browsable as one set without duplicating content that needs to stay in sync.
